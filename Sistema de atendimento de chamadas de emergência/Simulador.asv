clear all;
close all;


[Start1,Type1,HandlingTime1] = importfile('Dados G31.txt',2, 1465);
% formatIn = '"HH:mm:ss.SSS"';
% exp=zeros(length(Start1),1);
% for i=1:length(Start1)
% exp(i)=datenum(14:30:09.990, '"HH:mm:ss.SSS"');
% end
%[ HandlingTime ] = Conversor( HandlingTime1,Start );

Start=zeros(length(Start1),6);
for i=1:length(Start1)
    Start(i,:)=datevec(Start1(i));
end
